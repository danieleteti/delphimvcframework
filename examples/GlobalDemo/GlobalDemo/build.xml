<project name="MyProject" default="compile" basedir=".">
  <target name="compile" description="Compiling..." >
    <exec executable="cmd">
      <arg value="/c"/>
      <arg value="runmsbuild.bat"/>
      <arg value="-p"/>
    </exec>
    <exec command="pasdoc.exe --output docs --name DelphiMSBuild2 --write-uses-list MainForm.pas ReceiverForm.pas">
    </exec>             
  </target>
</project>
